# Golden Ranger Online

1. Install Node.js.
2. Run `npm install`
3. Run `npm start`
4. Open the displayed web address.
5. Create a room and share the 6-character room code with friends.

For public internet play, deploy this folder to a Node.js host that supports WebSockets and set its start command to `npm start`.
